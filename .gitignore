__pycache__/
*.py[cod]
.venv/
venv/
.env

# Model weights are downloaded or trained, never committed.
*.pt
*.pth

# Nothing derived from the footage goes in the repository. The match is real and
# the recordings stay off any public service, so frames, annotated video, tiled
# datasets and the validation batches Ultralytics renders are all excluded.
inference_out/
training_runs/
runs/
datasets/
*.mp4
*.avi
*.mkv
tracks.json
